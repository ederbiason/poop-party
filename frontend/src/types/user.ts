export type User = {
    _id: string
    email: string
    password: string
    name: string
    parties: string[]
    createdAt: string
    updatedAt: string
    partyWins: number
    profileImage: string
}
